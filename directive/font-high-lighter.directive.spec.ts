import { FontHighLighterDirective } from './font-high-lighter.directive';

describe('FontHighLighterDirective', () => {
  it('should create an instance', () => {
    const directive = new FontHighLighterDirective();
    expect(directive).toBeTruthy();
  });
});
